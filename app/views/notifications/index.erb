<!-- app/views/notifications/index.html.erb -->
<h1>Expiring Contracts</h1>
<ul>
  <% @expiring_contracts.each do |contract| %>
    <li><%= contract.title %> - Expires on <%= contract.end_date.strftime('%Y-%m-%d') %></li>
  <% end %>
</ul>
